<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      autoReload="true"
      internalLogLevel="Warn"
      internalLogFile="${basedir}/Logs/nlog-internal.log">

  <extensions>
    <add assembly="NLog.Web.AspNetCore" />
  </extensions>

  <targets async="true">
    <target xsi:type="File"
            name="controllerFile"
            fileName="${basedir}/Logs/Controllers/${event-properties:item=controller:whenEmpty=System}.log"
            layout="${longdate:universalTime=true}|${level:uppercase=true}|${message}${newline}"
            archiveEvery="Day"
            archiveNumbering="Date"
            maxArchiveFiles="14"
            concurrentWrites="true"
            keepFileOpen="false"
            encoding="utf-8" />

    <target xsi:type="Console" name="console" layout="${longdate:universalTime=true}|${level:uppercase=true}|${logger}|${message}" />
  </targets>

  <rules>
    <logger name="*" minlevel="Info" writeTo="controllerFile,console" />
  </rules>
</nlog>
